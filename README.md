To inspect the mcp server
npx @modelcontextprotocol/inspector python main.py

To run locally
uv run fastmcp run main.py

To add it i to cluade desktop
uv run fastmcp install claude-desktop main.py --config-path "<Claude-folder-path>"